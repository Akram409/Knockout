import useClass from "../../Hooks/useClass";

const Classes = () => {
    const [classes] = useClass()
    console.log(classes)
    return (
        <div>
            
        </div>
    );
};

export default Classes;